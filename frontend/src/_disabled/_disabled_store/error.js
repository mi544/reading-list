const state = () => ({
  error: null,
  errorMessage: null,
})
const mutations = {}
const actions = {}
const getters = {}

export const errorModule = {
  namespaced: true,
  state,
  mutations,
  actions,
  getters,
}
