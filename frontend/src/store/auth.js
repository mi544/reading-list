const state = () => ({
  isAuthenticated: null,
  username: null,
  name: null,
})
const mutations = {}
const actions = {}
const getters = {}

export const authModule = {
  state,
  mutations,
  actions,
  getters,
}
